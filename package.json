{
  "name": "clamber-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "main": "index.js",
  "scripts": {
    "install": "npm install --workspaces",
    "build-frontend": "npm run build --workspace=client",
    "start": "node index.js"
  }
}
